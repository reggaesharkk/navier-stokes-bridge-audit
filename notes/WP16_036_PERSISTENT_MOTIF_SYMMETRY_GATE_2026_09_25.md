# WP16 Exact [223]+[123]→[036] Symmetry Gate

Resolve the persistent ordered partner motif into exact signed wavevector triads and quotient by simultaneous coordinate permutations and sign flips (the cubic signed-permutation group).

Target:
[
[2,2,3]_{m advector}+[1,2,3]_{m advected}	o[0,3,6]_{m output}.
]

For each continuation step N8→N9, N9→N10, N10→N11, report the exact triad count, number of symmetry classes, and each class's coefficient envelope, signed transfer before/after, transfer change, and coherence.

Finite diagnostic only.
