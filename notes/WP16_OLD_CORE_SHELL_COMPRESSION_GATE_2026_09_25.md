# WP16 Old-Core Shell Compression Gate

**Prince Upadhyay, Independent Research — 25 September 2026**

The N=11 block decomposition found a near-factorized finite mechanism:

- old-core phase corrections primarily deplete positive stretching;
- new-shell phases primarily increase signed H2 transfer.

This gate asks whether the 2,084-dimensional old-core correction is itself compressible into a small number of radial shell blocks.

For each inherited mode, define

[
s(k)=lceil |k|\rceil.
]

The registered wrapped old-core correction is split into shell-restricted directions

[
Deltaphi^{(s)}.
]

Each shell is evaluated:

1. alone at full registered amplitude;
2. at a small fraction (arepsilon=0.1) to estimate directional changes in
   [
   log C,quad log N_2^{>2},quad log P_+;
   ]
3. cumulatively after ranking shell blocks by endpoint log-C gain.

The decisive question is whether a small subset of shells recovers most of the old-core (P_+) depletion and quotient gain.

A positive result would compress the recursive denominator-relaxation step into a much lower-dimensional shell rule. A negative result would show that the old-core effect is more distributed across mode geometry.

Finite N=11 diagnostic only.
