# WP16 Phase-Path and Old/New Block Decomposition Gate

**Prince Upadhyay, Independent Research — 25 September 2026**

**Status:** prospective finite-state mechanism audit.

The weighted-triad and physical-space depletion gates established that the rising N=11 quotient is not explained by a newly opened shell carrying disproportionate signed transfer, and that positive stretching is broadly redistributed/depressed rather than merely clipped at the extreme tail.

This gate asks which phase block actually causes that improvement.

## Phase decomposition

Let (phi^{m inh}) be the inherited N=10 optimized phase map embedded into the N=11 support, and let (phi^{m opt}) be the final N=11 optimizer.

Define the shortest wrapped displacement

[
Deltaphi
=
operatorname{Arg}
e^{i(phi^{m opt}-phi^{m inh})}.
]

Split it into:

- **old-core corrections:** entries on the 2,084 inherited active pairs;
- **new-shell phases:** entries on the 703 newly activated pairs.

## Paths

Evaluate

[
phi(lambda)
=
phi^{m inh}
+
lambda,Deltaphi_{mathcal B},
qquad
0lelambdale1,
]

for three blocks (mathcal B):

1. old-core only;
2. new-shell only;
3. full correction.

Record along each path:

- (N_2^{>2});
- (P_+=langle(omegacdot Somega)_+angle);
- (C_infty^{m stretch});
- global triadic coherence.

## Endpoint test

Refine four endpoints on (96^3):

- inherited;
- old-core corrections only;
- new-shell phases only;
- full optimized state.

The endpoint comparison directly answers whether the denominator depletion is driven mainly by:

- inherited-core relaxation;
- new-shell phases;
- or a nonlinear interaction between the two.

A log-gain interaction statistic is included:

[
I
=
lograc{C_{m full}}{C_{m inh}}
-
lograc{C_{m old}}{C_{m inh}}
-
lograc{C_{m new}}{C_{m inh}}.
]

## Scope boundary

This is a finite N=11 path decomposition. It does not establish a general variational principle, asymptotic divergence, or a Navier–Stokes regularity/singularity theorem.
