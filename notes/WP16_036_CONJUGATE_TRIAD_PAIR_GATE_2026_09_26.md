# WP16 Dominant Conjugate-Triad-Pair Gate

The isolated motif has already reduced to one dominant ([036]) conjugate phase coordinate,

[
k=pm(6,0,3).
]

That coordinate contains four exact ordered motif terms. Group them by the exact conjugacy

[
(p,q,k)sim(-p,-q,-k).
]

This produces the conjugate-triad-pair classes inside the one-angle coordinate.

For each cutoff step, record:

- number of exact ordered terms;
- number of conjugate-pair classes;
- coefficient envelope (A) of each class;
- signed transfer before and after the registered update;
- (Delta N);
- share of the total one-angle envelope;
- share of the one-angle registered gain.

Primary test:

[
	ext{Does one conjugate pair carry more than }99.999%	ext{ of the one-angle coefficient envelope?}
]

No new PDE simulation is needed. This is exact accounting of the already-generated finite triad data.
